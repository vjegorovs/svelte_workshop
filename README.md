## Svelte WebSocket chat app built during JSNation Live workshop

# Learned basics of sveltejs, fiddled with websockets

Steps:

- npx degit sveltejs/template svelte-app
- cd svelte-app
- npm install
- npm run dev
